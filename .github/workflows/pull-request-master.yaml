name: "Test"

on:
  pull_request:
    branches:
      - main

jobs:
  run_tests:
    name: Verify PR
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Read .nvmrc
        run: echo "##[set-output name=NVMRC;]$(cat .nvmrc)"
        id: nvm
      - name: Save version
        run: echo "latest" > version
      - name: Use Node.js (.nvmrc)
        uses: actions/setup-node@v1
        with:
          node-version: "${{ steps.nvm.outputs.NVMRC }}"
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"
      - uses: actions/cache@v2
        id: yarn-cache # use this to check for `cache-hit` (`steps.yarn-cache.outputs.cache-hit != 'true'`)
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}-v3
          restore-keys: |
            ${{ runner.os }}-yarn-
      - name: Install deps
        run: yarn
        env:
          YARN_CACHE_FOLDER: ".yarn-cache"
      - name: Run affected unit tests
        run: yarn test/affected --base HEAD~ --head HEAD
       - name: Coverage affected
        if: always()
        run: yarn test/coverage/affected --base HEAD~ --head HEAD
      - name: Compile affected apps
        run: yarn compile/affected --base HEAD~ --head HEAD
        env:
          MQTT_CONNECTION: ${{ secrets.MQTT_CONNECTION }}
          GAMING_ROOM_GAMING_PC_USERNAME: "${{ secrets.GAMING_ROOM_GAMING_PC_USERNAME }}"
          NODE_ENV: production
