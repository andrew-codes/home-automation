name: "Deploy"

on:
  push:
    branches:
      - main

jobs:
  deploy_amd64_apps:
    name: Deploy amd64 Apps
    runs-on: amd64-runner
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Read .nvmrc
        run: echo "##[set-output name=NVMRC;]$(cat .nvmrc)"
      - name: Get latest tag
        run: echo "##[set-output name=TAG_SHA;]$(git rev-list --tags --max-count=1)"
        id: tag_sha
      - name: Use Node.js (.nvmrc)
        uses: actions/setup-node@v1
        with:
          node-version: "${{ steps.nvm.outputs.NVMRC }}"
      - name: Install deps
        run: yarn
      - name: Compile changed apps
        run: yarn lerna run compile --stream --since "${{ steps.tag_sha.outputs.TAG_SHA }}"
      - name: Deploy
        run: lerna run deploy --stream --since "${{ steps.tag_sha.outputs.TAG_SHA }}"