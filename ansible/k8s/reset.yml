---
- hosts: mains
  become: yes
  become_method: sudo
  become_user: root
  gather_facts: yes
  connection: ssh
  remote_user: hl

  tasks:
    - name: Reset cluster
      command: kubeadm reset --force
- import_playbook: install.yml
