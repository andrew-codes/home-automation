---
- hosts: mains
  become: yes
  become_method: sudo
  become_user: hl
  gather_facts: yes
  connection: ssh
  remote_user: hl

  tasks:
    - name: Schedule 5 day rolling backup of gaming namespace
      shell: velero schedule create gaming-daily-backup --schedule "0 7 * * *" --ttl "720h" --include-namespaces gaming