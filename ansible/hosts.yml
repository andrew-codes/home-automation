all:
  vars:
    ansible_user: hl
  children:
    cluster_primary:
      hosts:
        192.168.1.45:
    mains:
      hosts:
        192.168.1.45:
    workers:
      hosts:
    pihole:
      hosts:
        192.168.1.3:
