all:
  vars:
    ansible_user: hl
  children:
    windows:
      vars:
        vars:
        ansible_winrm_server_cert_validation: ignore
        ansible_port: 5986
        ansible_winrm_transport: basic
      hosts:
        192.168.1.25:
    gaming_pc:
      vars:
        ansible_winrm_server_cert_validation: ignore
        ansible_port: 5986
        ansible_winrm_transport: basic
      hosts:
        192.168.1.25:
    windows_dev:
      vars:
        vars:
        ansible_winrm_server_cert_validation: ignore
        ansible_port: 5986
        ansible_winrm_transport: basic
      hosts:
