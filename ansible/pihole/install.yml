---
- hosts: pihole
  become: yes

  tasks:
    - name: Installing Prerequisites
      apt:
        name:
          - curl
        state: present

    - name: Install pihole
      become_user: root
      become_method: sudo
      become: yes
      shell: curl -sSL https://install.pi-hole.net | bash /dev/stdin --unattended
