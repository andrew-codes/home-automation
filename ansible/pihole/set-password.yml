---
- hosts: pihole
  become: yes
  vars_files:
    - .secrets/pihole.yml

  tasks:
    - name: Set password
      become_user: root
      become_method: sudo
      become: yes
      shell: pihole -a -p "{{ pihole_password }}"
