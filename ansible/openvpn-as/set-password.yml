---
- hosts: openvpn
  become: yes
  vars_files:
  - .secrets/openvpn.yml

  tasks:
    - name: Change user password
      user:
        name: openvpn
        update_password: always
        password: "{{ openvpn_password|password_hash('sha512') }}"
