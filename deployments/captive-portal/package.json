{
  "private": true,
  "name": "@ha/captive-portal",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "deploy": "scripts/bin/deploy.sh",
    "postdeploy": "kubectl rollout restart deployment captive-portal",
    "initialize-secrets": "scripts/bin/initialize-secrets.sh"
  },
  "nx": {
    "targets": {
      "deploy": {
        "outputs": [],
        "dependsOn": [
          {
            "target": "image/push",
            "projects": "dependencies"
          }
        ]
      }
    }
  },
  "dependencies": {
    "@ha/captive-portal-app": "^0.0.1",
    "@ha/mqtt": "^0.0.1",
    "@ha/secrets": "^0.0.1"
  }
}
