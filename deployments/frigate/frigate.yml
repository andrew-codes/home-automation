---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: frigate-media-pv-volume
  namespace: default
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 60Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data/frigate"

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frigate-media-claim
  namespace: default
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 60Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frigate
  namespace: default
  labels:
    app.kubernetes.io/name: frigate
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: frigate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: frigate
      annotations:
        vault.hashicorp.com/agent-inject: 'true'
        vault.hashicorp.com/role: app
        vault.hashicorp.com/agent-inject-status: update
        vault.hashicorp.com/agent-inject-secret-env.sh: kv/frigate
        vault.hashicorp.com/agent-inject-template-env.sh: |
          {{ with secret "kv/mqtt" }}
          export MQTT_USERNAME="{{ .Data.USERNAME }}"
          export MQTT_PASSWORD="{{ .Data.PASSWORD }}"
          {{ end }}
          {{ with secret "kv/k8s" }}
          export CLUSTER_IP="{{ .Data.CLUSTER_IP }}"
          {{ end }}
          {{ with secret "kv/frigate" }}
              {{ range $k, $v := .Data }}
          export {{ $k }}="{{ $v }}"
              {{ end }}
          {{ end }}
    spec:
      serviceAccountName: app
      containers:
        - name: frigate
          image: docker-registry:5000/frigate
          imagePullPolicy: Always
          command:
            - "sh"
          args:
            - "-c"
            - "source /vault/secrets/env.sh && /start.sh"
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
            - name: rtmp
              containerPort: 1935
              protocol: TCP
          volumeMounts:
            - name: media-frigate
              mountPath: /media/frigate
          securityContext:
            privileged: true
            allowPrivilegeEscalation: true
      volumes:
        - name: media-frigate
          persistentVolumeClaim:
            claimName: frigate-media-claim