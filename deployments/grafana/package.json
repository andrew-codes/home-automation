{
  "private": true,
  "name": "@ha/grafana",
  "version": "0.0.1",
  "scripts": {
    "postinstall": "jb install",
    "deploy": "scripts/bin/deploy.sh",
    "postdeploy": "kubectl rollout restart deployment grafana"
  },
  "dependencies": {
    "@ha/metrics": "^0.0.1"
  }
}
