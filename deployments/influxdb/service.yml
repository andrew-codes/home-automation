apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: default
  labels:
    app: app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8086
      targetPort: http
  selector:
    app: app
