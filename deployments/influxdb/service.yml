apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: default
spec:
  type: NodePort
  ports:
    - name: influxdb
      port: 8086
      protocol: TCP
      targetPort: 8086
  selector:
    name: influxdb
