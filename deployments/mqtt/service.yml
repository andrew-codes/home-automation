apiVersion: v1
kind: Service
metadata:
  name: mqtt
  namespace: default
  labels:
    app: app
spec:
  type: NodePort
  ports:
    - name: default-port
      port: 1883
      targetPort: default
    - name: web-socket-port
      port: 9001
      targetPort: web-socket
  selector:
    app: app
