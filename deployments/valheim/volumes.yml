apiVersion: v1
kind: PersistentVolume
metadata:
  name: valheim-pv-volume
  namespace: gaming
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/valheim
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valheim-pv-claim
  namespace: gaming
  labels:
    app: valheim-server
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi