{
  "private": true,
  "name": "@ha/guest-pin-codes",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "deploy": "scripts/bin/deploy.sh",
    "initialize-secrets": "scripts/bin/initialize-secrets.sh",
    "postdeploy": "kubectl rollout restart deployment guest-pin-codes "
  },
  "nx": {
    "targets": {
      "deploy": {
        "outputs": [],
        "dependsOn": [
          {
            "target": "image/push",
            "projects": "dependencies"
          }
        ]
      }
    }
  },
  "dependencies": {
    "@ha/guest-pin-codes-app": "^0.0.1",
    "@ha/mqtt": "^0.0.1",
    "@ha/secrets": "^0.0.1"
  }
}
