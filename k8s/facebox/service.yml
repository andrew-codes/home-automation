apiVersion: v1
kind: Service
metadata:
  name: facebox
  namespace: home-automation
  labels:
    app.kubernetes.io/name: facebox
spec:
  type: NodePort
  ports:
    - name: web
      port: 8080
      protocol: TCP
      targetPort: web
  selector:
    app.kubernetes.io/name: facebox