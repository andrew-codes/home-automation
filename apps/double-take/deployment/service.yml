apiVersion: v1
kind: Service
metadata:
  name: double-take
  namespace: default
  labels:
    app.kubernetes.io/name: double-take
spec:
  type: NodePort
  ports:
    - name: web
      port: 3000
      protocol: TCP
      targetPort: web
  selector:
    app.kubernetes.io/name: double-take