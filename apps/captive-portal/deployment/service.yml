apiVersion: v1
kind: Service
metadata:
  name: captive-portal
  namespace: default
  labels:
    app.kubernetes.io/name: captive-portal
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: captive-portal
