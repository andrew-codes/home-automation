{
  "private": true,
  "name": "@ha/home-assistant-config-applicator",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "cp -r src/ dist/",
    "image/build": "docker build -t docker.smith-simms.family:5000/home-assistant-config-applicator:latest --build-arg BUILD_ARCH=amd64 --build-arg CLI_VERSION=4.3.0 .",
    "image/push": "docker push docker.smith-simms.family:5000/home-assistant-config-applicator:latest"
  }
}
