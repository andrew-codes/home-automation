{
  "root": "apps/infrastructure/docker-registry",
  "sourceRoot": "apps/infrastructure/docker-registry",
  "projectType": "application",
  "targets": {
    "clean": {
      "executor": "@nrwl/workspace:run-commands",
      "outputs": [
        ".terraform",
        ".terraform.lock.hcl",
        ".terraform.tfstate.lock.info",
        "terraform.tfstate"
      ],
      "options": {
        "command": "rm -rf .terraform .terraform.lock.hcl .terraform.tfstate.lock.info terraform.tfstate",
        "cwd": "apps/infrastructure/docker-registry"
      }
    }
  },
  "tags": [],
  "implicitDependencies": []
}
