rest_command:
  update_valheim_ddns:
    url: !secret VALHEIM_DDNS_REST_COMMAND

automation:
  - alias: Update Valheim DNS
    trigger:
      - platform: time_pattern
        hours: "/1"
    action:
      - service: rest_command.update_valheim_ddns
