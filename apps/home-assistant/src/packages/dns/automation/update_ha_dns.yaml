shell_command:
  update_ha_dns: !secret HA_DDNS_REST_COMMAND

automation:
  - alias: Update Home Assistant DNS
    id: update_home_assistant_dns
    trigger:
      - platform: time_pattern
        hours: "/1"
    action:
      - service: shell_command.update_ha_dns
