shell_command:
  update_vpn_dns: !secret VPN_DDNS_REST_COMMAND

automation:
  - alias: Update VPN DNS
    id: update_vpn_dns
    trigger:
      - platform: time_pattern
        hours: "/1"
    action:
      - service: shell_command.update_vpn_dns