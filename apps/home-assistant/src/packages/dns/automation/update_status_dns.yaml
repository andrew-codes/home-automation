shell_command:
  update_status_dns: !secret STATUS_DDNS_REST_COMMAND

automation:
  - alias: Update Uptime Status DNS
    id: update_status_assistant_dns
    trigger:
      - platform: time_pattern
        hours: "/1"
    action:
      - service: shell_command.update_status_dns
