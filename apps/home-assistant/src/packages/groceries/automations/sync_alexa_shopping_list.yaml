automation:
  - alias: Sync Alexa shopping list
    id: groceries_sync_alexa_shopping_list
    trigger:
      - platform: time_pattern
        minutes: "/1"
    action:
      - service: mqtt.publish
        data:
          topic: /home/shopping-list/refresh
