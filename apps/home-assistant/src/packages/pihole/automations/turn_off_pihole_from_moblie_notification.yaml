automation:
  - alias: Turn off pihole from mobile notification
    id: turn_off_pihole_from_mobile_notification
    trigger:
      platform: event
      event_type: mobile_app_notification_action
      event_data:
        actionName: DISABLE_PIHOLE
    action:
      - service: switch.turn_off
        entity_id: switch.pi_hole
