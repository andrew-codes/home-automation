shell_command:
  update_vpn_dns: !secret HA_DDNS_REST_COMMAND

automation:
  - alias: Update Home Assistant DNS
    trigger:
      - platform: time_pattern
        hours: "/1"
    action:
      - service: shell_command.update_ha_dns