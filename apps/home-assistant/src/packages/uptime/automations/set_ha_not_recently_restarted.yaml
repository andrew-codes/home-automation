automation:
  - alias: Set HA not recently restarted
    id: set_ha_not_recently_restarted
    trigger:
      - platform: state
        entity_id: sensor.uptime_home_assistant
    action:
      - delay:
          minutes: 5
      - service: input_boolean.turn_on
        target:
          entity_id: input_boolean.ha_not_recently_restarted
