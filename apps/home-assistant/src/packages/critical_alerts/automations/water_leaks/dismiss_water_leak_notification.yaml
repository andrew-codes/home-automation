automation:
  - alias: Dismiss water leak alert
    id: dismiss_water_leak_alert
    trigger:
      platform: event
      event_type: ios.notification_action_fired
      event_data:
        actionName: WATER_LEAK_NON_ISSUE
    action:
      - service: switch.turn_on
        entity_id: switch.shutoff_valve
