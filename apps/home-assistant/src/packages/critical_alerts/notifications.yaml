ios:
  push:
    categories:
      - name: Critical Alert
        identifier: critical_alert_water
        actions:
          - identifier: WATER_LEAK_NON_ISSUE
            title: "Not an Issue: Dismiss"
            activationMode: background
            authenticationRequired: false
            destructive: true
            behavior: default

notify:
  - name: ALL_DEVICES
    platform: group
    services:
      - service: mobile_app_andrew_s_iphone
      - service: mobile_app_dorri_s_iphone
