import { GameCollectionDefinition } from "../../types"
import continuePlaying from "./continuePlaying"
import all from "./all"

const collectionDefinitions: GameCollectionDefinition[] = [continuePlaying, all]

export default collectionDefinitions
